/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'cdn-icons-png.flaticon.com', 'drive.google.com', 'w7.pngwing.com']
      }
  };

export default nextConfig;
